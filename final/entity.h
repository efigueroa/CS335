//Object class that we are going to use to create any entities on screen
class entity
{
  private:
  int x, y;                                         //Two ints to give the  object's position on screen
  
  public:
  
  entity();
   
  int  move(x, y);                                   //function to detect movement
       
  
};
